# ? Database Configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/interencheres?useUnicode=true
spring.datasource.username=root
spring.datasource.password=AZerty2010--
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? Hibernate Properties
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ? Data Initialization (Replaces deprecated `spring.datasource.initialization-mode`)
spring.sql.init.mode=always

# ? Server Port (Optional - Change if needed)
server.port=8080
